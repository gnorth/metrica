# Metrika for Android

Native WebView shell for the private Metrika site.

## Local build

Use Android Studio, open the `android` directory, then run the `app` configuration.

## GitHub build

The repository workflow builds a signed debug APK. Open the latest successful
**Build Android APK** run and download the `Metrika-APK` artifact.
